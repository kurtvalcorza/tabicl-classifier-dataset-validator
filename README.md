# tabicl-classifier-dataset-validator

DIMER dataset validator for TabICLv2 classification. Validates CSV datasets before fine-tuning and writes the DIMER validation `result.json`.

Pairs with `tabicl-classifier-finetuner`. The umbrella documentation lives in `tabicl-classifier-pipeline`.
